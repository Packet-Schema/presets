node_modules
dist

# Generated by scripts/build.ts; also gitignored.
src/presets.generated.ts

# 184 authored PSDL documents. Prettier reflows YAML in ways that fight the
# column alignment these use to keep bit layouts readable, and the 4-stage
# gate — not a formatter — is what decides whether they are well-formed.
presets
